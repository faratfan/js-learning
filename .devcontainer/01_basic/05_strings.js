const name = "Fann";
const repoCount = 2;
//console.log(name + repoCount);//older way--> string concatenation
//the other way is string Interpolation-->${} creating palce holder with ``backticks
console.log(`Hello my name is ${name} and my GitHub repo count is ${repoCount}`);
